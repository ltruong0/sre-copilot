{
  "servers": [
    {
      "name": "netbox",
      "description": "NetBox DCIM/IPAM server for querying network infrastructure",
      "command": "/Users/ltruong/Desktop/projects/netbox-watson-copilot/netbox-mcp-server/.venv/bin/netbox-mcp-server",
      "args": [],
      "env": {
        "NETBOX_URL": "${NETBOX_URL}",
        "NETBOX_TOKEN": "${NETBOX_TOKEN}"
      },
      "enabled": false
    }
  ]
}
