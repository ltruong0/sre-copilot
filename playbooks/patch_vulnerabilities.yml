---
- name: Patch Security Vulnerabilities
  hosts: "{{ target_hosts }}"
  gather_facts: false
  become: true

  tasks:
    - name: Apply security patches
      debug:
        msg: "Vulnerabilities patched, no new vulnerabilities to report."
